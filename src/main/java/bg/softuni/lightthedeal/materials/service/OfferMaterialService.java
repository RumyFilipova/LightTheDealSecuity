package bg.softuni.lightthedeal.materials.service;

public class OfferMaterialService {



   // OfferMaterial offerMaterial = OfferMaterial.builder()
    //        .offer(offer)
     //       .material(material)
      //      .quantity(quantity)
       //     .priceAtTimeOfOffer(BigDecimal.valueOf(material.getSinglePrice())) // snapshot!
        //    .build();
}
